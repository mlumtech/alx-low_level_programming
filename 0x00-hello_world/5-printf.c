#include <stdio.h>

/**
 * main - print the string in main function
 *
 * Description: using the main function
 * This programe prints "with proper grammar, but the outcome is a piece of art"
 * Return: 0
 */
 int main(void)
 {
	 prinf("with proper grammar, but the outcome is a piece of art,\n");
	 return (0);
}

